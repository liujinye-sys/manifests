name: CLA Check

on:
  pull_request:
    types: [opened, reopened, synchronize]
  issue_comment:
    types: [created]

permissions:
  contents: read
  pull-requests: write
  statuses: write

jobs:
  cla-check:
    uses: liujinye-sys/public-actions/.github/workflows/cla.yml@dev
    secrets: inherit
